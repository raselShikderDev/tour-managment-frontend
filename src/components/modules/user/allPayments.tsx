// import { ErrorAlert } from "@/components/error"
// import { LoadingSpinner } from "@/components/loading"
// import { useGetAllBookingsQuery } from "@/redux/features/bookings/booking.api"

const AllPayments = () => {

    // const {data:allPayments, error, isLoading} = useGetAllBookingsQuery()

    // if (isLoading) {
    //     return <LoadingSpinner/>
    // }

    // if (error) {
    //     return <ErrorAlert/>
    // }

  return (
    <div>
      <h1>Payments for user</h1>
    </div>
  )
}

export default AllPayments
